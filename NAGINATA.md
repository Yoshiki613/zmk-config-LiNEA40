# LiNEA40-mato式 × 薙刀式 v17

**全レイヤーの図解 → [KEYMAP.md](KEYMAP.md)**

ベースは **LiNEA40-mato式**（最新 workflow のキーマップ）。レイヤー0〜6 は mato式のままです。

## 変更点（mato式からの差分）

| 項目 | 内容 |
|------|------|
| `west.yml` | `zmk-naginata-v17` を追加 |
| レイヤー0 左下（位置30） | `⇄薙刀`（L7トグル） |
| レイヤー7 | 薙刀式（QWERTY物理位置 + 調整済み親指） |
| コンボ | H+J かなON / F+G かなOFF |

## レイヤー0 ⇔ レイヤー7

| 操作 | 動作 |
|------|------|
| **左手いちばん下**（位置30） | L0 ⇔ L7 トグル |
| H + J（位置15+16） | かなON → L7 |
| F + G（位置13+14） | かなOFF → L0 |
| L7 の左手いちばん下 | L0 へ戻る |

## レイヤー7の親指（調整済み）

| 位置 | キー |
|------|------|
| 左手 34・35 | `ngSpace` / `ngEnter`（入れ替え済み） |
| 右手いちばん左（36） | `Backspace` |
| 右手 37 | `ngSpace` |

※ 上3段は QWERTY 物理位置の `&ng`（mato印字とは無関係）

### 薙刀式のカスタム（zmk-naginata-v17）

| キー | 動作 |
|------|------|
| `U` 単独 | Backspace |
| `T` 単独 | ↓ |
| `Y` 単独 | ↑ |
| `I` 単独 | る |
| `O` 単独 | す |
| `Space` + `U` | さ |
| `Space` + `I` | よ |
| `Space` + `O` | え |
| `Space` + `T` / `Y` | Shift+← / Shift+→（選択移動） |
| `V` + `M` 同時押し | Enter |
| L7 親指右下 | ← / →（通常 `&kp`） |

### 定型文（連続シフト U+I を保持してから）

| 操作 | 出力 |
|------|------|
| U+I 保持 → R | 承知しました。 |
| U+I 保持 → F | 確認いたします。 |

※ U+I は**同時押しで保持**（単独で U→BS、I→る を出してからだと定型文にならない）

## ビルド

GitHub Actions でビルド。`config/LiNEA40.keymap` が使用されます。
