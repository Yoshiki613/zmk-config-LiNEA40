# LiNEA40-mato式 × 薙刀式 v17 × せおと配列

**全レイヤーの図解 → [KEYMAP.md](KEYMAP.md)**

ベースは **LiNEA40-mato式**（最新 workflow のキーマップ）。レイヤー0〜6 は mato式のままです。

## 変更点（mato式からの差分）

| 項目 | 内容 |
|------|------|
| `west.yml` | `zmk-naginata-v17` を追加 |
| レイヤー0 左下（位置30） | `⇄薙刀`（L7トグル） |
| レイヤー0 左下2つ目（位置31） | `⇄せおと`（L12トグル） |
| レイヤー7 | 薙刀式（QWERTY物理位置 + 調整済み親指） |
| レイヤー12 | せおと配列（ローマ字入力用・数字段なし） |
| コンボ | H+J かなON / F+G かなOFF |

## レイヤー0 ⇔ レイヤー7

| 操作 | 動作 |
|------|------|
| **左手いちばん下**（位置30） | L0 ⇔ L7 トグル |
| **左下2つ目**（位置31） | L0 ⇔ L12 トグル |
| H + J（位置15+16） | かなON → L7 |
| F + G（位置13+14） | かなOFF → L0 |
| L7 の左手いちばん下 | L0 へ戻る |
| L12 の左手いちばん下 | L0 へ戻る |

## レイヤー7の親指（調整済み）

| 位置 | キー |
|------|------|
| 左手 34・35 | `ngSpace` / `ngEnter`（入れ替え済み） |
| 右手いちばん左（36） | `Backspace` |
| 右手 37 | `ngSpace` |

※ 上3段は QWERTY 物理位置の `&ng`（mato印字とは無関係）

### 薙刀式のカスタム（zmk-naginata-v17）

| キー | 動作 |
|------|------|
| `U` 単独 | Backspace |
| `T` 単独 | ↓ |
| `Y` 単独 | ↑ |
| `I` 単独 | る |
| `O` 単独 | す |
| `Space` + `U` | さ |
| `Space` + `I` | よ |
| `Space` + `O` | え |
| `Space` + `T` / `Y` | Shift+← / Shift+→（選択移動） |
| `V` + `M` 同時押し | Enter |
| L7 親指右下 | ← / →（通常 `&kp`） |

### 定型文（連続シフト U+I を保持してから）

| 操作 | 出力 |
|------|------|
| U+I 保持 → R | 承知しました。 |
| U+I 保持 → F | 確認いたします。 |

※ U+I は**同時押しで保持**（単独で U→BS、I→る を出してからだと定型文にならない）

## レイヤー12 — せおと配列

[『せおと配列』の記事](https://note.com/firstyear_dev/n/ne4aedaad09b2) を元に、文字3段のみを実装しています。数字段は実装していません。

```
F Z H P ←      → YO O W YA
D S N T G      L A I U E
B R M C>K K    Y ,  YU . V
```

ローマ字拡張はキーボード側で送出します。

| 位置 | 送出 |
|------|------|
| C | K |
| X | YA |
| J | YU |
| Q | YO |

Space 長押し中は、右上に書かれている裏入力を送出します。`LANGUAGE_1` / `LANGUAGE_2` はPC側の入力モードと、キーボード側のせおと面を同時に切り替えます。

| Space + 位置 | 送出 |
|--------------|------|
| L の上 | LANGUAGE_1 + SEOTO |
| A の上 | LANGUAGE_2 + SEOTO_EN |
| W | ~ |
| L | ' |
| A | Backspace |
| I | NN（ん） |
| U | -（長音） |
| E | XTU（っ） |

英字入力用の `SEOTO_EN` では、ローマ字拡張を使わず `C/Q/X/J` をそのまま送出します。

## レイヤー15〜17

`config/LiNEA40.keymap` では、薙刀式用サブレイヤーのコピーを追加しています。

| レイヤー | 内容 |
|----------|------|
| 15 | L9 Number_NG のコピー |
| 16 | L10 ShortCut_NG のコピー |
| 17 | L11 Function_NG のコピー |

## ビルド

GitHub Actions でビルド。`config/LiNEA40.keymap` が使用されます。
